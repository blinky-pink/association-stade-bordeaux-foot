{% extends 'base.html.twig' %}

{% block title %}Nouveau responsable{% endblock %}

{% block body %}
<h1>Ajouter un responsable</h1>

<div class="form-wrapper">
    {{ form(form) }}
    <div class="show-actions">
        <a href="{{ path('responsable_list') }}" class="btn btn-secondary">Annuler</a>
    </div>
</div>
{% endblock %}
