{% extends 'base.html.twig' %}

{% block title %}Nouveau message{% endblock %}

{% block body %}
<h1>Ajouter un message</h1>

<div class="form-wrapper">
    {{ form(form) }}
    <div class="show-actions">
        <a href="{{ path('message_list') }}" class="btn btn-secondary">Annuler</a>
    </div>
</div>
{% endblock %}
