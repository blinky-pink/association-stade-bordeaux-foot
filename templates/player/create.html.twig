{% extends 'base.html.twig' %}

{% block title %}Nouveau joueur{% endblock %}

{% block body %}
<h1>Ajouter un joueur</h1>

<div class="form-wrapper">
    {{ form(form) }}
    <div class="show-actions">
        <a href="{{ path('player_list') }}" class="btn btn-secondary">Annuler</a>
    </div>
</div>
{% endblock %}
